import React from 'react';
import { Widget } from 'react-chat-widget';
import 'react-chat-widget/lib/styles.css';

const Chat = (props) => {

    return(
        <div>
            <Widget />
        </div>
    );
    
}

export default Chat;